@{
    ViewData["Title"] = "Inicio";
}

<h2 class="fw-bold">Panel principal</h2>

<p class="text-muted">
    Bienvenido <strong>@User.FindFirst("Nombre")?.Value</strong>
</p>

<p>
    <strong>Rol:</strong> @User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value
</p>
